package com.translater.service.utill;
/*

import com.mongodb.MongoClient;
import com.translater.service.model.user.User;
import com.translater.service.model.user.UserRole;
import com.translater.service.service.Impl.UserService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.mongodb.core.MongoOperations;
import org.springframework.data.mongodb.core.MongoTemplate;
import org.springframework.data.mongodb.core.query.Query;
import org.springframework.stereotype.Component;

import static org.springframework.data.mongodb.core.query.Criteria.where;


public class MongoTest {

    @Autowired
    UserService userService;

    public static void main(String[] args) throws Exception {

        MongoOperations mongoOps = new MongoTemplate(new MongoClient(), "database");
        mongoOps.insert(new User("Ura", "45454", true, UserRole.ROLE_ADMIN));
        System.out.println(mongoOps.findOne(new Query(where("username").is("Ura")), User.class));
        mongoOps.dropCollection("user");



    }
}
*/